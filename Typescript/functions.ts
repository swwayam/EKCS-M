const add = (num1: number, num2: number): number => {
        return num1 + num2
}

console.log(add(10, 11));


const add2 = (num1: number, num2: number, name: string): string => {
        return num1 + num2 + name
}


console.log(add2(10, 11, "Masood"));

export { }

// 21Masood